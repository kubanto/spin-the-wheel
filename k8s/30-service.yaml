apiVersion: v1
kind: Service
metadata:
  name: spin-the-wheel
  namespace: spin-the-wheel
spec:
  type: NodePort
  selector:
    app: spin-the-wheel
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 31080
